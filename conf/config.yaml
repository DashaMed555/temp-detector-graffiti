defaults:
  - data_loading: normal_dataset
  - model: model
  - freeze_layers: freeze_layers
  - fine_tuning: fine_tuning
  - logging: logging
  - inference: inference
  - onnx_converter: onnx_converter

paths:
  train_json: "${data_loading.root}/train/annotations.json"
  val_json: "${data_loading.root}/valid/annotations.json"
  test_json: "${data_loading.root}/test/annotations.json"
  train_images: "${data_loading.root}/train/images"
  val_images: "${data_loading.root}/valid/images"
  test_images: "${data_loading.root}/test/images"

params:
  class_names: ["graffiti", "vandalism"]
