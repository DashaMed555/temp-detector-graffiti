# 🎨 Детектор граффити с использованием Grounding DINO

**Авторы:** Гоенко Алина, Медведева Дарья

---

## 🎯 Постановка задачи

Детектор граффити должен распознавать рисунки и надписи на стенах зданий, разделять их на **хорошие** и **плохие**, а также выдавать детекции пользователю. Это полезно для своевременного выявления порчи городской среды.

## 📥 Формат данных

### Входные данные

- Одно **3-канальное изображение**.
- Размерность: от **400x400** до **1920x1080**.

### Выходные данные

Детекции, которые содержат в себе:

- Координаты ограничительных рамок (**bboxes**) в формате `x0, y0, x1, y1`.
- Строковую метку одного из классов.
- Уверенность предсказания (confidence) от **0 до 1**.

---

## 📈 Метрики

Для определения качества работы детектора будут использованы **precision**, **recall** и **F1-score**.

| Метрика       | Описание                                        | Ожидаемый результат |
| ------------- | ----------------------------------------------- | ------------------- |
| **Precision** | Насколько стоит доверять детекциям.             | **> 80%**           |
| **Recall**    | Как часто детектор пропускает объекты.          | **> 80%**           |
| **F1-score**  | Среднее гармоническое между precision и recall. | **> 80%**           |

---

## 🧪 Валидация

Все датасеты будут совмещены в один большой датасет, который затем будет разделён на:

- **Тренировочную** выборку (80%)
- **Валидационную** выборку (10%)
- **Тестовую** выборку (10%)

Эти выборки будут сохранены в отдельных директориях: `train`, `val` и `test`. Для проведения валидации будут считываться все изображения и разметка к ним из директории `val`, таким образом данные на валидации всегда будут теми же самыми, что обеспечит их воспроизводимость.

---

## 📊 Данные

В качестве данных мы будем использовать размеченные фотографии граффити со стен зданий и других уличных объектов. Разметка будет хранится в трёх файлах: `train_annotations.json`, `val_annotations.json`, `test_annotations.json`.

**Структура JSON-объекта:**

```json
{
  "image_name": "000000.png", // название изображения
  "width": 640, // ширина изображения
  "height": 480, // длина изображения
  "annotations": [
    // список всех bboxes на этом изображении
    {
      "label_name": "graffiti", // класс объекта
      "cx": 0.231836, // центр bboxes объекта по оси x
      "cy": 0.716281, // центр bboxes объекта по оси y
      "w": 0.071703, // ширина bboxes объекта
      "h": 0.145437 // высота bboxes объекта
    }
  ]
}
```

**Источники данных (Roboflow):**

- **Надписи:**
  - [Graffiti Detection](https://universe.roboflow.com/mpl2/graffiti-detection-shzgr/) (70 изображений, 124 объектов вандализма)
- **Арты и надписи:**
  - [17k Graffiti](https://universe.roboflow.com/detr-2bavb/17k-graffiti/) (7 641 изображение, 14 703 объектов граффити),
  - [train](https://universe.roboflow.com/thais-souza/train-tj6ju/) (707 изображений: 386 объектов граффити, 703 объекта вандализма)

---

## 🧠 Моделирование

### Бейзлайн

Простейшее решение — это запуск модели **Grounding DINO** без fine-tuning.

### Основная модель

**Grounding DINO** — open-set objects detection модель.

- **Swin энкодер:** преобразует изображения в визуальные признаки.
- **BERT текст-энкодер:** преобразует текстовые запросы в эмбеддинги.
- **Слои трансформера:** сопоставляют текстовые и визуальные признаки.

Модель вычисляет семантическое соответствие между регионами изображения и элементами текстового запроса, формируя представления, на которых основываются предсказания.
🔗 [Репозиторий модели на Hugging Face](https://huggingface.co/IDEA-Research/grounding-dino-base)

### Обучение

Для обучения будет использована библиотека `transformers`. Из неё будут взяты классы `Trainer` и `TrainingArgs`. Будут изменены некоторые функции, использующиеся для обучения, чтобы адаптировать модель Grounding DINO под метод `train` класса `Trainer`.

---

## 🚀 Внедрение

Детектор будет представлен в виде пакета, который можно будет установить через `pip`.

---

## 🛠 Установка и настройка (Setup)

Этот раздел поможет вам подготовить рабочее окружение для разработки, обучения и запуска инференса. Мы используем **Poetry** для управления зависимостями и **DVC** для версионирования тяжелых данных.

### 1. Системные требования (Prerequisites)

Перед началом убедитесь, что в вашей системе установлены:

- **Python 3.12+**
- **Poetry**
- **Git**

### 2. Клонирование и настройка среды

Сначала клонируйте репозиторий и установите все зависимости, включая инструменты разработки и библиотеки для работы с нейросетями:

```bash
# Клонирование репозитория
git clone https://github.com/DashaMed555/temp-detector-graffiti.git
cd detector-graffiti

# Установка зависимостей через Poetry
poetry install

```

### 3. Настройка контроля качества (Pre-commit)

Мы используем **pre-commit hooks** для автоматической проверки кода (линтеры `flake8`, `black`, `isort` и проверки безопасности). Чтобы они работали локально при каждом коммите, выполните:

```bash
# Активация хуков
poetry run pre-commit install

# (Опционально) Проверка всех файлов вручную
poetry run pre-commit run --all-files

```

### 4. Настройка данных (DVC & Google Drive)

Поскольку датасеты и веса моделей слишком велики для Git, мы используем **DVC**. Данные хранятся в облаке Google Drive.

**Получение доступа:**
Для получения `CLIENT_ID` и `CLIENT_SECRET` обратитесь по адресу: [d.medvdeveda@g.nsu.ru](mailto:d.medvdeveda@g.nsu.ru).

**Конфигурация и загрузка:**

```bash
# 1. Настройка удаленного хранилища (локально)
poetry run dvc remote modify gdrive --local gdrive_client_id "<CLIENT_ID>"
poetry run dvc remote modify gdrive --local gdrive_client_secret "<CLIENT_SECRET>"

# 2. Загрузка данных
poetry run dvc pull

# 3. Распаковка данных (ОБЯЗАТЕЛЬНО)
# DVC загружает данные в архивах. Распакуйте их для работы скриптов:
unzip "data/*.zip" -d data/
unzip "datasets/*.zip" -d datasets/

```

> [!IMPORTANT]
> При первом выполнении `dvc pull` откроется окно браузера для OAuth2-авторизации. Войдите в предоставленный технический аккаунт Google.

---

### 5. Проверка готовности (Quick Check)

Чтобы убедиться, что всё настроено корректно, попробуйте запустить визуализацию данных или пустой прогон инференса:

- **Проверка данных:**

```bash
poetry run graffiti-detector validate data_validation.num_samples=5

```

- **Проверка CLI:**

```bash
poetry run graffiti-detector --help

```

---

**Что делать дальше?**

- Если вы хотите обучить модель, перейдите к разделу Обучение (Fine-tuning).
- Для запуска детекции на изображениях используйте команду `inference`.

---

## Подготовка данных (Preprocessing)

Перед запуском обучения необходимо подготовить датасет. Весь процесс управляется пакетом `convert_dataset`.

### 🛠 Шаг A: Слияние и балансировка датасетов (`process_datasets`)

Этот этап отвечает за объединение разрозненных наборов данных в единую структуру, пригодную для обучения Grounding DINO, с одновременной балансировкой классов.

#### Основные возможности скрипта:

1. **Глобальная переименовка**: Файлы переименовываются в формат `000001.jpg`, `000001.txt` и т.д. Это предотвращает конфликты имен при слиянии данных из разных источников (например, если в двух датасетах есть файл `img_1.jpg`).
2. **Процентная выборка (`percentage`)**: Позволяет взять лишь часть исходных данных (например, 10% для быстрой проверки пайплайна).
3. **Анализ BBox-статистики**: Скрипт сканирует YOLO-аннотации и подсчитывает количество объектов каждого класса внутри каждого изображения.
4. **Умная балансировка классов**: Если балансировка включена, алгоритм стремится достичь соотношения **1:1** между объектами «граффити» и «вандализм» не по количеству картинок, а по количеству **bounding boxes**.

#### Алгоритм балансировки:

Скрипт использует жадный алгоритм:

- Подсчитывается общее количество объектов меньшинства (minority class).
- Картинки перемешиваются и добавляются в итоговую выборку до тех пор, пока количество объектов обоих классов не достигнет целевого значения (target).
- Это позволяет избежать перекоса модели в сторону более часто встречающегося класса.

#### Запуск:

Для стандартного запуска используйте команду:

```bash
poetry run graffiti-detector process_datasets

```

#### Переопределение параметров (Hydra):

Вы можете изменять поведение скрипта напрямую из терминала без правки YAML-файлов:

- **Изменение объема данных (взять 50%):**

```bash
poetry run graffiti-detector process_datasets data_processing.params.percentage=0.5

```

- **Отключение балансировки:**

```bash
poetry run graffiti-detector process_datasets data_processing.params.balance_classes=False

```

- **Смена выходной папки:**

```bash
poetry run graffiti-detector process_datasets data_processing.paths.output_name="custom_dataset"

```

#### Результат работы:

В целевой директории (по умолчанию `datasets/dataset`) создается структура:

- `images/` — переименованные файлы изображений.
- `labels/` — соответствующие им YOLO-аннотации.
- `data.yaml` — служебный файл с описанием путей и имен классов для обучения.

**Доступные параметры для настройки:**

- `data_processing.paths.input_dir` — папка с исходными датасетами (default: `"data"`)
- `data_processing.paths.output_root` — корень для сохранения нового датасета (default: `"datasets"`)
- `data_processing.paths.output_name` — имя результирующей папки (default: `"dataset"`)
- `data_processing.params.run_type` — какую выборку брать из исходных данных (default: `"train"`)
- `data_processing.params.percentage` — доля изображений для обработки, от 0.0 до 1.0 (default: `1.0`)
- `data_processing.params.balance_classes` — включать ли балансировку классов (default: `True`)

---

### 🛠 Шаг B: Разделение выборки (`split_dataset`)

После слияния данных их необходимо разделить на непересекающиеся подмножества для обучения, валидации и финального тестирования. Скрипт `split_dataset.py` автоматизирует этот процесс, гарантируя сохранность пар «изображение-аннотация».

#### Основные возможности и логика:

1. **Пропорциональное разделение**: По умолчанию данные делятся в соотношении **80/10/10%** (Train/Valid/Test). Сумма долей строго контролируется — если она не равна 1.0, скрипт выдаст ошибку.
2. **Воспроизводимость (Seed)**: Используется фиксированный параметр `seed` из конфигурации. Это гарантирует, что при повторном запуске файлы распределятся точно так же, что критически важно для сравнения результатов разных экспериментов.
3. **Синхронизация файлов**: Скрипт перемешивает список изображений и автоматически подтягивает соответствующие им `.txt` файлы аннотаций.
4. **Обработка пустых аннотаций**: Если для изображения отсутствует файл разметки, скрипт создаст пустой `.txt` файл. Это необходимо для корректной работы YOLO-совместимых загрузчиков данных.
5. **Изоляция данных**: Перед началом работы скрипт очищает старые папки разделения, исключая смешивание данных из разных запусков.

#### Структура директорий после выполнения:

Скрипт преобразует плоскую структуру в иерархическую, готовую для обучения:

```text
datasets/dataset/
├── train/
│   ├── images/
│   └── labels/
├── valid/
│   ├── images/
│   └── labels/
└── test/
    ├── images/
    └── labels/

```

#### Запуск:

```bash
poetry run graffiti-detector split_dataset

```

#### Настройка через Hydra:

Вы можете переопределить параметры разделения прямо в командной строке:

- **Изменение пропорций (например, без тестовой выборки):**

```bash
poetry run graffiti-detector split_dataset \
  data_splitting.train_ratio=0.9 \
  data_splitting.val_ratio=0.1 \
  data_splitting.test_ratio=0.0

```

- **Изменение seed для другого случайного разбиения:**

```bash
poetry run graffiti-detector split_dataset data_splitting.seed=42

```

- **Выбор конкретного датасета для разделения:**

```bash
poetry run graffiti-detector split_dataset data_loading.name="my_new_data"

```

#### Безопасность и валидация:

Скрипт выполняет ряд проверок перед началом копирования:

- Проверка наличия исходных папок `images/` и `labels/`.
- Проверка поддержки расширений файлов (напр. `.jpg`, `.png`).
- Проверка на "пустой" датасет.

---

На основе кода скрипта `convert_yolo_to_json.py`, вот детальное описание для вашего README. Оно объясняет переход от формата YOLO к формату, специфичному для современных трансформеров детекции, таких как Grounding DINO.

---

**Доступные параметры для настройки:**

- `data_loading.name` — название датасета, который нужно разделить (default: `"dataset"`)
- `data_splitting.train_ratio` — доля обучающей выборки (default: `0.8`)
- `data_splitting.val_ratio` — доля валидационной выборки (default: `0.1`)
- `data_splitting.test_ratio` — доля тестовой выборки (default: `0.1`)
- `data_splitting.seed` — фиксированный Random Seed для воспроизводимости (default: `42`)

---

### 🛠 Шаг C: Конвертация в JSON (`convert_yolo_to_json`)

Модели архитектуры Grounding DINO требуют единого файла аннотаций в формате JSON вместо множества текстовых файлов. Скрипт `convert_yolo_to_json.py` выполняет финальную сборку данных, объединяя информацию о изображениях и их разметке.

#### Основные функции скрипта:

1. **Сбор метаданных**: С помощью утилиты `get_image_dimensions` скрипт автоматически определяет ширину и высоту каждого изображения. Это критически важно, так как координаты YOLO являются нормализованными, и для корректного обучения модели необходимо знать реальное разрешение кадра.
2. **Денормализация и сопоставление**: Скрипт читает YOLO `.txt` файлы и преобразует индексы классов (0, 1) в человекочитаемые имена (`graffiti`, `vandalism`), используя настройки из конфига.
3. **Формирование единого индекса**: Для каждого подмножества (`train`, `valid`, `test`) создается отдельный файл `annotations.json`, содержащий полный список изображений и соответствующих им объектов.
4. **Статистический отчет**: По завершении работы скрипт выводит в лог итоговое распределение объектов по классам для каждой выборки.

#### Структура выходного JSON:

Каждая запись в файле представляет собой объект следующего вида:

```json
{
  "image_name": "000001.jpg",
  "width": 1024,
  "height": 768,
  "annotations": [
    {
      "label_name": "vandalism",
      "cx": 0.512, // Центр по X (0-1)
      "cy": 0.345, // Центр по Y (0-1)
      "w": 0.12, // Ширина (0-1)
      "h": 0.21 // Высота (0-1)
    }
  ]
}
```

#### Запуск:

```bash
poetry run graffiti-detector convert_yolo_to_json

```

#### Настройка через Hydra:

Вы можете динамически изменять параметры конвертации:

- **Смена имен классов (если вы адаптируете модель под другой проект):**

```bash
poetry run graffiti-detector convert_yolo_to_json \
  params.class_names="['art', 'damage']"

```

- **Выбор другого датасета:**

```bash
poetry run graffiti-detector convert_yolo_to_json \
  data_loading.name="another_dataset"

```

#### Почему это важно:

В отличие от стандартного YOLO-пайплайна, Grounding DINO использует текстовые описания классов как промпты. Данный скрипт подготавливает данные таким образом, чтобы визуальные признаки и текстовые метки могли быть корректно сопоставлены в Feature Fusion слоях модели.

---

**Следующий шаг:**
После генерации JSON-файлов данные полностью готовы к этапу обучения (`Train`). Рекомендуется также запустить скрипт `validate.py` для визуальной проверки наложенных боксов из созданного JSON.

На основе кода `validate.py` подготовил подробное описание этого этапа для вашего README. Этот раздел объясняет, как убедиться в качестве данных перед тем, как тратить ресурсы на долгое обучение модели.

---

**Доступные параметры для настройки:**

- `data_loading.name` — имя папки датасета для конвертации (default: `"dataset"`)
- `params.class_names` — список имен классов для маппинга (default: `["graffiti", "vandalism"]`)

---

### 🛠 Шаг D: Визуальная валидация (`validate`)

Прежде чем запускать обучение на несколько часов или дней, критически важно убедиться, что аннотации в формате JSON были сформированы корректно и соответствуют изображениям. Скрипт `validate.py` позволяет визуально проверить разметку на случайной выборке данных.

#### Что делает этот скрипт:

1. **Денормализация координат**: Скрипт считывает нормализованные значения (`cx`, `cy`, `w`, `h`) из JSON и пересчитывает их в пиксельные координаты на основе реального разрешения каждого конкретного изображения.
2. **Цветовая кодировка**: Для наглядности классы разделены по цветам:

- **Синий**: Graffiti (граффити).
- **Красный**: Vandalism (вандализм).

3. **Интерактивный просмотр**: Скрипт использует `matplotlib` для отображения кадров и делает паузу после каждого изображения, ожидая нажатия клавиши **Enter**, чтобы вы могли внимательно изучить детали.
4. **Проверка границ (Clipping)**: Алгоритм автоматически корректирует (обрезает) рамки, если они выходят за границы изображения, и логирует подозрительные объекты.

#### Запуск:

По умолчанию скрипт берет случайные 10 изображений из валидационной выборки:

```bash
poetry run graffiti-detector validate

```

#### Настройка через Hydra:

Вы можете гибко управлять процессом валидации из терминала:

- **Проверка тестовой выборки (вместо валидационной):**

```bash
poetry run graffiti-detector validate data_validation.run_type="test"

```

- **Увеличение количества проверяемых образцов:**

```bash
poetry run graffiti-detector validate data_validation.num_samples=25

```

- **Сохранение результатов на диск**: Если вы работаете на сервере без графического интерфейса, можно сохранять отрисованные картинки в папку:

```bash
poetry run graffiti-detector validate data_validation.save_dir="plots/validation_results"

```

#### Зачем это нужно:

Этот шаг помогает выявить типичные проблемы подготовки данных:

- **Смещение рамок**: Возникает при ошибках в расчете `width/height` изображения.
- **Инверсия классов**: Если при конвертации перепутались индексы `graffiti` и `vandalism`.
- **Пустые аннотации**: Проверка того, что изображения без объектов действительно не имеют отрисованных боксов.

---

**Завершение этапа Data Preparation:**
Если визуальная проверка прошла успешно и рамки плотно облегают нужные объекты, можно переходить к следующему разделу — **🚀 Train (Обучение)**.

---

**Доступные параметры для настройки:**

- `data_loading.name` — на каком датасете проверять разметку (default: `"dataset"`)
- `data_validation.run_type` — какую выборку визуализировать: `train`, `valid` или `test` (default: `"train"`)
- `data_validation.num_samples` — количество случайных изображений для показа (default: `10`)
- `params.class_names` — список имен классов (default: `["graffiti", "vandalism"]`)
- `data_validation.save_dir` — папка для сохранения отрисованных результатов (default: `"reports/visualizations"`)

---

## Конфигурация обучения (Hydra)

Система использует **Hydra** для управления гиперпараметрами. Все настройки обучения находятся в `conf/fine_tuning/` и `conf/freeze_layers/`.

- **Custom Loss:** Модель оптимизирует как классификацию, так и регрессию боксов.
- **Заморозка слоев:** Вы можете обучать модель целиком или заморозить энкодер, оставив только головы детекции (`bbox_embed`).
- **Динамические пути:** Благодаря OmegaConf, пути к данным подставляются автоматически в зависимости от выбранного датасета.

---

## 🚀 Запуск обучения (Train)

> [!IMPORTANT] > **Важное условие:** Весь процесс обучения и логирования метрик завязан на **MLflow**.
> Прежде чем запускать команду `train`, необходимо запустить сервер MLflow в отдельном окне терминала, иначе процесс обучения будет сопровождаться предупреждениями.

Для запуска обучения используется единая консольная утилита `graffiti-detector`. Выполните следующие шаги:

#### Шаг 1: Запуск сервера логирования (Терминал №1)

Откройте новый терминал в папке проекта и запустите MLflow:

```bash
poetry run mlflow ui --port 8888

```

_Оставьте этот терминал открытым. Сервер должен работать на протяжении всего обучения._

#### Шаг 2: Запуск тренировки (Терминал №2)

Вернитесь в основной терминал и запустите обучение:

```bash
# Базовый запуск
poetry run graffiti-detector train

```

#### Запуск с переопределением параметров:

Вы можете менять датасет, количество эпох или скорость обучения прямо из терминала:

```bash
# Обучение на большом датасете (5 эпох)
poetry run graffiti-detector train \
  data_loading=big_dataset \
  fine_tuning.num_train_epochs=5 \
  fine_tuning.learning_rate=2e-5

```

---

## Мониторинг и метрики (MLflow)

В процессе обучения используется кастомный `MLflowCallback`, который автоматически логирует:

- **Параметры:** Гиперпараметры и хэш Git-коммита.
- **Метрики:** Loss, Precision, Recall и F1-score на каждой эпохе.
- **Артефакты:** Графики обучения (`loss_plot.png`, `f1_plot.png`).

**Для просмотра результатов выполните:**

```bash
poetry run mlflow ui --port 8888

```

Затем откройте [http://localhost:8888](https://www.google.com/search?q=http://localhost:8888) в браузере.

---

### Резюме команд CLI для Train

| Действие                   | Команда                                                             |
| -------------------------- | ------------------------------------------------------------------- |
| **Полный цикл подготовки** | См. шаги 1 (A, B, C)                                                |
| **Запуск Fine-tuning**     | `poetry run graffiti-detector train`                                |
| **Смена конфига данных**   | `poetry run graffiti-detector train data_loading=difficult_dataset` |
| **Запуск MLflow UI**       | `poetry run mlflow ui`                                              |

---

## 🚀 Production Preparation & Deployment

В этом разделе описаны шаги по подготовке обученной модели к интеграции в рабочую среду, процессы оптимизации и состав финальной сборки.

### 1. Оптимизация и конвертация (ONNX)

Для обеспечения высокой производительности, снижения задержек (latency) и независимости от Python-инфраструктуры, модель переводится из формата PyTorch в формат **ONNX**.

- **Преимущества:** Оптимизация графа вычислений и возможность запуска через высокопроизводительный движок `ONNX Runtime`.
- **Динамические оси:** Модель экспортируется с поддержкой `dynamic axes` для параметра `batch_size`, что позволяет обрабатывать пакеты изображений переменного размера.
- **Совместимость:** Используется `opset_version=19`, поддерживающая современные операторы архитектуры Transformer.

**Запуск конвертации:**
Укажите путь к дообученному чекпоинту в конфиге `onnx_converter.yaml` и выполните команду:

```bash
poetry run graffiti-detector convert

```

---

### 2. Комплектация поставки (Artifacts)

Для успешного запуска модели в production-окружении пакет поставки должен включать следующие компоненты:

| Тип артефакта       | Файл / Модуль            | Описание                                                             |
| ------------------- | ------------------------ | -------------------------------------------------------------------- |
| **Модель**          | `grounding_dino.onnx`    | Оптимизированный бинарный файл модели.                               |
| **Веса (Legacy)**   | `pytorch_model.bin`      | Исходные веса (если планируется использование через PyTorch).        |
| **Конфигурация**    | `conf/inference/`        | YAML-файлы с порогами детекции (`threshold`) и текстовыми промптами. |
| **Среда**           | `pyproject.toml`         | Файл зависимостей Poetry для воспроизведения окружения.              |
| **Инференс-модуль** | `inference/inference.py` | Класс `Inference` для пре- и пост-процессинга данных.                |

---

### 3. Инференс и эксплуатация

Готовая модель поддерживает гибкую настройку через CLI, что позволяет адаптировать её под конкретные аппаратные мощности (CPU/GPU) без изменения кода.

- **Текстовое управление:** Работа модели в режиме Zero-shot или Fine-tuned требует задания классов через промпт (например, `"graffiti . vandalism ."`).
- **Пост-процессинг:** Используется встроенный метод `post_process_grounded_object_detection` для маппинга нормализованных координат на реальное разрешение исходных изображений.

**Пример запуска финальной модели:**

```bash
# Запуск инференса с оптимизированными параметрами
poetry run graffiti-detector inference \
  model=ft_model \
  inference.threshold=0.4 \
  inference.device=cuda

```

---

### 4. Мониторинг и версионирование

Перед развертыванием каждой новой версии модели в production, рекомендуется провести финальную проверку артефактов:

1. **Проверка через MLflow:** Убедиться, что метрики `eval_precision` и `eval_f1` соответствуют целевым показателям.
2. **DVC Sync:** Проверить наличие актуальной версии модели в удаленном хранилище Google Drive для возможности быстрого отката (rollback).
3. **Validation Check:** Запустить `validate.py` на контрольной выборке `difficult_dataset` для визуальной проверки.

---

## 🔍 Inference (Предсказание)

Этот раздел описывает процесс запуска обученной модели на новых данных для детектирования граффити и вандализма.

### 1. Подготовка данных для инференса

Для запуска предсказаний необходимо подготовить директорию с изображениями.

- **Поддерживаемые форматы:** `.jpg`, `.jpeg`, `.png`.
- **Структура:** Модель принимает на вход папку с изображениями. Аннотации для инференса не требуются.
- **Пример данных:** Вы можете загрузить тестовый набор данных («артефакт») из нашего хранилища через DVC:

```bash
# Загрузка примера данных для теста инференса
poetry run dvc pull datasets/dataset.zip
unzip datasets/dataset.zip -d test_samples/

```

### 2. Конфигурация

Инференс управляется через систему **Hydra**. Основные параметры находятся в `conf/inference/`. Перед запуском вы можете изменить:

- `inference.threshold`: порог уверенности модели (по умолчанию `0.25`).
- `model`: путь к вашей дообученной модели (чекпоинту) или использование базовой.
- `inference.output_dir`: путь, куда будут сохранены визуализированные результаты.

### 3. Запуск предсказания (CLI)

Код инференса вынесен в отдельный модуль `inference/inference.py`, что минимизирует зависимости от тренировочного цикла и позволяет использовать его как самостоятельную утилиту.

**Основная команда:**

```bash
poetry run graffiti-detector inference

```

**Примеры использования с переопределением параметров:**

```bash
# Запуск на конкретной папке с низким порогом обнаружения
poetry run graffiti-detector inference \
  inference.input_dir="test_samples/" \
  inference.threshold=0.2

# Использование вашей лучшей дообученной модели
poetry run graffiti-detector inference \
  model=ft_model \
  model.checkpoint_path="outputs/best_model.bin"

```

### 4. Оптимизированный инференс (ONNX)

Для работы в средах с ограниченными ресурсами или без Python-зависимостей (например, в production), рекомендуется использовать конвертацию в ONNX. Это позволяет запускать модель через `ONNX Runtime`.

1. **Экспорт модели:**

```bash
poetry run graffiti-detector convert

```

2. **Результат:** В корне проекта появится файл `grounding_dino.onnx`, готовый к интеграции в высокопроизводительные системы.

### 5. Выходные данные и визуализация

После завершения работы скрипта в директории `outputs/inference/` (или указанной вами) будут созданы:

- **Изображения с отрисовкой:** Копии исходных фото с BBox-ами (синий для `graffiti`, красный для `vandalism`).
- **Лог-файл (`inference.log`):** Список всех найденных объектов, их координат и показателей уверенности (confidence score).

---
